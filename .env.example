# Headless mode (set to false to watch the browser)
HEADLESS=true

# First login credentials (required)
FIRST_EMAIL=
FIRST_PASSWORD=

# Second login credentials (optional; used if a second login page appears)
SECOND_EMAIL=
SECOND_PASSWORD=

# Optional: override selectors if needed
GENERATE_SELECTOR=button:has-text("Generate token")
# TOKEN_SELECTOR=[data-testid="token"]
